define host{
	use		linux-server
        host_name               localhost
        alias                   localhost (mon.codeshelf.com)
        address                 127.0.0.1
	hostgroups		device
        }

