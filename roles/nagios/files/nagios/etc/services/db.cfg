define servicegroup {
        servicegroup_name       db
        alias                   Services for database servers
}

define service {
        use                     generic-service
        servicegroups           db
        hostgroup_name          db
        service_description     PostgreSQL
        check_command           check_nrpe!check_pgsql
        }

