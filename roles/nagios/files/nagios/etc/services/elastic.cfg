define servicegroup {
        servicegroup_name       elastic
        alias                   Services for Elasticsearch
}

define service {
        use                     generic-service
        servicegroups           elastic
        hostgroup_name          elastic
        service_description     elasticsearch
        check_command           check_elasticsearch  
        }

